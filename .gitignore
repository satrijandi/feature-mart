# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
*.egg-info/

# dbt artefacts, if the generated project is ever run in place. The showcase
# directs its own runs into showcase/ instead, so nothing here appears during
# an ordinary `make -C showcase` cycle.
transform/target/
transform/dbt_packages/
transform/logs/
transform/.user.yml

# OS
.DS_Store
