# ============================================================================
# GENERATED FILE - DO NOT EDIT BY HAND
#   Merged from the `relations:` block of every feature spec.
#   Edit a spec and run `make generate`.
# ============================================================================
version: 2
sources:
- name: bronze_events
  description: Upstream append-only source feeding 1 table(s).
  schema: '{{ env_var(''FS_BRONZE_SCHEMA'', ''bronze_events'') }}'
  tables:
  - name: customer_login
    description: 'Immutable append-only event log.

      The feature store relies on rows for a past event_date never changing once written; the reusable
      daily partial layer is only sound while that holds.

      Consumed by: fact_agg_features_login_device_v1, fact_agg_features_login_history_v2'
    loaded_at_field: _scd_valid_from
    freshness:
      warn_after:
        count: 12
        period: hour
      error_after:
        count: 36
        period: hour
